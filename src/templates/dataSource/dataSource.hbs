package {{snakeName}}

import (
	"context"
	"github.com/hashicorp/terraform-plugin-sdk/v2/helper/schema"
	"github.com/hashicorp/terraform-plugin-sdk/v2/diag"
	{{#unless skeletonStructure}}
	"fmt"
	"time"

	"github.com/hashicorp/terraform-plugin-sdk/v2/helper/retry"
		
	gcloud "terraform-provider-genesyscloud/genesyscloud"
	{{/unless}}
)
/*
   The data_source_genesyscloud_{{snakeName}}.go contains the data source implementation
   for the resource.
*/

// dataSource{{pascalName}}Read retrieves by name the id in question
func dataSource{{pascalName}}Read(ctx context.Context, d *schema.ResourceData, meta interface{}) diag.Diagnostics {
	{{#unless skeletonStructure}}
	sdkConfig := meta.(*gcloud.ProviderMeta).ClientConfig
	proxy := new{{pascalName}}Proxy(sdkConfig)

	name := d.Get("name").(string)

	return gcloud.WithRetries(ctx, 15*time.Second, func() *retry.RetryError {
		{{mainObjectCamel}}Id, retryable, err := proxy.get{{pascalName}}IdByName(ctx, name)

		if err != nil && !retryable {
			return retry.NonRetryableError(fmt.Errorf("Error {{englishName}} %s: %s", name, err))
		}

		if retryable {
			return retry.RetryableError(fmt.Errorf("No {{englishName}} found with name %s", name))
		}

		d.SetId({{mainObjectCamel}}Id)
		return nil
	})
	{{/unless}}
	{{#if skeletonStructure}}
	return nil
	{{/if}}
}
